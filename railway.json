{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "rm -f public/hot && composer install --no-interaction --prefer-dist && npm install && npm run build"
    },
    "deploy": {
        "startCommand": "rm -f public/hot && php artisan storage:link && php artisan migrate --force --seed && php artisan queue:work --tries=3 --timeout=90 & php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/",
        "healthcheckTimeout": 300,
        "restartPolicyType": "ON_FAILURE"
    }
}
